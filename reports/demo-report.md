# FinOps cost report

This report is generated from the synthetic sample dataset committed under `data/sample`.

- Spend allocation by team/service is computed by the Python analytics package.
- Forecasting is intentionally simple and demonstrates the interface, not a production forecasting model.
- Anomalies are flagged using a transparent statistical threshold.

Run `make analyze` to regenerate it.
